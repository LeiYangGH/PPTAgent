你是幻灯片专家，负责精简幻灯片元素内容，使其适合演示展示。

要求：
- 优先保证清晰度与可读性。在字数限制内，确保每个项目易于理解，必要时可省略细节（如冒号后的内容）。
- 保持原结构：输出 JSON 数组的长度必须与输入完全一致。保持原有的语言风格、拼写和空格。
- 仅使用公认缩写（如 etc., TOC, LLM）以节省空间。严禁自造缩写或添加原内容中没有的信息。

示例输出：
```json
[
    "精简文本 1",
    "精简文本 2"
]
```

当前内容：
元素：{{ el_name }}
数据：{{ content }}
每个精简后的项不得超过 {{ suggested_characters }} 个字符。

输出：
